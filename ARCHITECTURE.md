# ARCHITECTURE.md

Этот документ — контекст для AI-ассистентов (Cursor, Claude Code) и для разработчика. Кладётся в корень репозитория. В начале каждой сессии с AI добавляй этот файл в контекст.

---

## 1. Что это за система

Внутренняя CRM для компании, занимающейся строительством модульных домов. Команда до 10 человек. Каждый проект — индивидуальный (типовых готовых моделей нет, только шаблоны-заготовки по количеству модулей).

Главная задача системы — автоматизация полного цикла от лида до сдачи дома, с особым акцентом на **переговорный процесс с клиентом**: менеджер в разговоре должен видеть, как меняется цена при каждой правке, и объяснять клиенту "что почём".

Хоститься будет локально (внутренний сервер компании).

## 2. Роли пользователей

- **Менеджер** — продаёт, общается с клиентом, ведёт сделку от лида до договора. Работает в CRM большую часть дня.
- **Проектировщик** — рисует проекты в ArchiCAD, отправляет расчёты и PDF-планы в CRM через плагин. Коммерческие данные не видит.
- **Производство** — file-only роль для производственного доступа: видит разрешенные проектные файлы, но не меняет сделку и стоимость.
- **Админ** — редактирует справочники (позиции, цены, пользователи) через Django admin.
- **Руководитель** — видит всю воронку, аналитику, может править любые сделки.

## 3. Ключевые концепции

### Проект, сделка, версия
- **Deal (Сделка)** — основная сущность. Один клиент может иметь несколько сделок. Имеет `project_code` вида `3МД-Иван-Пулково` (уникальный бизнес-ключ).
- **ProjectVersion (Версия проекта)** — итерация расчёта. Каждая сделка имеет много версий, каждая версия — иммутабельный снэпшот конфигурации и цен на момент отправки клиенту.
- **Client (Клиент)** — физлицо или компания, заказчик.
- **ProjectFile (Файл проекта)** — метаданные файла в локальном workspace сделки; физический путь хранится относительно `CRM_FILES_ROOT`.

### Количество модулей
Метка "3МД", "5МД", "7МД" и т.д. означает количество модулей в доме (обычно от 1 до ~15, рекорд 11). Хранится как простое число `module_count` на Deal. UI и модель допускают `0` для ранних лидов без уточненного объема; API плагина ArchiCAD принимает 1..15. Это **не** ссылка на каталог моделей — каталога нет, каждый дом индивидуальный. Используется только для фильтрации и аналитики.

### Интеграция с ArchiCAD
Плагин ArchiCAD (пишется отдельно на C++) подключается к CRM по REST API. Отправляет:
- Структурированный payload (JSON) — список объектов дома: стены, окна, двери, полы, зоны, кровля с их guid'ами и параметрами
- PDF с планом этажа (один файл, одна страница)

в workspace я добавил папку из C:\Program Files\Graphisoft\API Development Kit 27.6003\Examples\Country она пока пустая. Там у нас будет плагин для archicad мы ее наполним из примеров (C:\Program Files\Graphisoft\API Development Kit 27.6003\Examples) и что то напишем сами

Каждая отправка создаёт новую `ProjectVersion`. Плагин шлёт детально (по объектам с guid'ами), а не суммарно, чтобы можно было показать **детальный diff** между версиями.

### Версионирование и diff
Когда приходит новая версия, сервер сравнивает её с предыдущей **по guid'ам объектов**. Выделяет изменения:
- Объект изменён (размер, тип) → модификация
- Объект добавлен → новый
- Объект удалён → убран

Каждое изменение автоматически превращается в стоимостную разницу ("окно W-02 уменьшилось с 2100×2300 на 1800×2300 → -45 000 ₽"). Это критичная фича системы — "что почём".

### Источник данных в полях
Каждое поле в конфигураторе имеет источник:
- `archicad` — прилетело из плагина, не трогалось
- `manual` — ввёл менеджер (цены от поставщиков, сауна)
- `override` — прилетело из ArchiCAD, но менеджер переписал

В UI источник показывается иконкой рядом с полем. Менеджер может редактировать любое поле.

### Отправка КП клиенту
Когда менеджер жмёт "Зафиксировать и отправить":
1. Текущая draft-версия фиксируется со статусом `sent_to_client`
2. Генерируется итоговый PDF: титулка + план из ArchiCAD + таблица расчёта
3. Автоматически создаётся новая draft-версия (копия отправленной) для дальнейшей работы
4. Отправленная версия больше не редактируется (хранится в истории)

### Наценка
Хранится как параметр (% на Deal), применяется при расчёте итога. Не зашита в формулы.

## 4. Модель данных (общий обзор)

```
Client
  - ФИО, телефон, email, участок (дополнительно введем поля для заполения договоров, актов итп)

Deal
  - project_code (unique)        "3МД-Иван-Пулково"
  - project_code_normalized      для поиска, lowercase+trim
  - code_client_name             часть кода проекта
  - code_site_name               часть кода проекта
  - module_count                  0..15 в CRM, 1..15 для ArchiCAD API
  - client                       FK → Client (nullable для orphan)
  - status                       'orphan'|'new'|'qualified'|'sent_quote'|...
  - assigned_manager             FK → User
  - margin_percent               декораж, по умолчанию 30
  - mortgage_required
  - target_deal_date
  - created_at, updated_at

ProjectVersion
  - deal                         FK → Deal
  - version_number               1, 2, 3...
  - source                       'archicad'|'manual'|'client_revision'
  - status                       'draft'|'sent_to_client'|'accepted'|'superseded'
  - frozen_data                  JSONB — вся конфигурация и цены
  - plan_pdf_path                путь к PDF от проектировщика
  - plan_preview_png_path        миниатюра первой страницы
  - plan_uploaded_at
  - quote_pdf_path               путь к итоговому КП клиенту (при sent_to_client)
  - quote_sent_at
  - created_by                   FK → User
  - created_at

ChangeLog
  - project_version              FK → ProjectVersion
  - changed_by                   FK → User
  - changed_at
  - field_path                   "items.window_W02.width"
  - old_value                    JSON
  - new_value                    JSON

ProjectFile
  - deal                         FK → Deal
  - project_version              nullable FK → ProjectVersion
  - source                       'client'|'designer'|'sales'|'system'
  - category                     'photo'|'pdf'|'dwg'|'other'
  - relative_path                путь относительно CRM_FILES_ROOT
  - original_name, size_bytes, mime_type, ext
  - uploaded_by, created_at, updated_at
  - is_archived, archived_at, archived_by

DealBathroom / DealBathroomLine
  - санузлы draft-версии проекта и строки наполнения из шаблона каталога
  - line_total = quantity * unit_price только для is_included=True

DealAdditionalOptionLine
  - дополнительные опции draft-версии проекта
  - по умолчанию шаблонные строки выключены

CostItem (справочник позиций, редактируется в админке)
  - code (unique)                "floor_insulation_200"
  - name_ru
  - unit                         'sqm'|'lm'|'pcs'|'complex'|'rubles'
  - category                     'floors'|'walls'|'openings'|...
  - price_material
  - price_work
  - formula_multiplier           опциональная формула, например "ceiling_height + 0.95"
  - is_active

CostItemOption
  - варианты комплектации для строки каталога (название, артикул/производитель, цена, единица)

Task (задачи менеджера)
  - deal                         FK → Deal
  - assignee                     FK → User
  - title
  - due_date
  - is_done
  - created_at

User (расширение стандартного Django User)
  - role                         'manager'|'designer'|'production'|'admin'|'head'

DirectMessage / Notification / AuditEvent
  - внутренние сообщения, уведомления и аудит действий
```

## 5. Стек

- **Backend:** Django 6 + Django REST Framework (для API плагина) + Django Channels.
- **DB:** PostgreSQL 16
- **Realtime:** Redis channel layer, WebSocket `/ws/events/`, события `notification.created` и `message.created`.
- **Frontend:** Django templates + HTMX + Bootstrap. Конфигуратор сейчас реализован серверной формой и частичными HTMX endpoint'ами.
- **Файловое хранилище:** локальная папка `CRM_FILES_ROOT` (по умолчанию `crm_files` в репозитории; на сервере можно вынести в `/var/crm_files/`).
- **Аутентификация:** Django стандартная для людей, DRF token для плагина.
- **Деплой:** Docker Compose (`app` + Postgres + Redis). Контейнер `app` запускает Daphne/ASGI; nginx/certbot не описаны в текущем compose-файле.

## 6. Ключевые бизнес-правила (что легко забыть)

1. `project_code` — уникальный, регистронезависимый для поиска. Автогенерируемый формат: `{модули}МД-{имя/компания}-{участок}`.
2. `module_count` — целое число 0..15 в текущей форме/модели. Не ссылка на каталог.
3. Каждая отправка из ArchiCAD = новая ProjectVersion. Всегда.
4. После отправки КП клиенту текущая draft замораживается, автоматически создаётся новая draft.
5. Замороженные версии (status != draft) **иммутабельны**. Никогда не редактируются.
6. Плагин не шлёт мебель — только конструктив, который влияет на цену.
7. Все цены при создании ProjectVersion копируются снэпшотом в frozen_data. Изменение CostItem в будущем **не меняет** старые версии.
8. Наценка — параметр на Deal, не в формуле.
9. Все роли видят разные наборы полей — проектировщик не видит коммерцию, file-only роли не меняют сделку/стоимость.
10. Файлы проекта архивируются мягко: физический файл переносится в `archive`, запись `ProjectFile` помечается `is_archived=True`.
11. Компактная карточка стоимости может вручную переписать totals draft-версии; исходные `config_inputs` меняет только полный расчет.

## 7. Что делать, если AI предлагает противоречащее этому документу

Отказываться. Этот документ — источник правды. Если появляется новое требование, сначала обновляется этот документ, потом код.

## 8. Версия документа

v1.0 — первая зафиксированная архитектура, сформулирована до начала кодирования.